{
  "name": "audit-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply audit-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply audit-db --local",
    "deploy:pages": "wrangler pages deploy frontend --project-name=geoscoreapp"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.90.0"
  },
  "dependencies": {
    "linkedom": "^0.18.9",
    "ulid": "^2.3.0"
  }
}
